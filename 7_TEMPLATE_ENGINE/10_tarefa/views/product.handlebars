<div>
    <a class="btn" href="/">Voltar</a>
    <div class="container">
        <div class="card">
            <h1 class="name">{{product.name}}</h1>
            <h2>Preço: {{product.price}}</h2>
        </div>
    </div>
</div>